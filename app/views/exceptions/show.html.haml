- content_for :title do
  = t('application.root.title_with_tag_line', tag_line: "#{@rescue_response}")
.row{ style: "margin-top:40px;"}
  .col-md-8.col-md-offset-2
    .center
      %h1= details[:name]
      %p= details[:message]
      %br
      %br
      = link_to "Back to home", root_path