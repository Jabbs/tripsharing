<span id="following_group_<%= resource.id %>">
	<% if current_user_following?(resource) %>
		<%= link_to "<i class='fa fa-check'></i> following".html_safe, "/#{resource.class.name.pluralize.downcase}/#{resource.slug}/followings/#{resource.followings.where(user_id: current_user.id).first.id}", method: :delete, remote: true, id: "star_#{resource.id}", title: "Unfollow this", class: "btn btn-turquoise btn-sm" %>
	<% else %>
		<%= link_to "follow", "/#{resource.class.name.pluralize.downcase}/#{resource.slug}/followings", method: :post, remote: true, id: "star_#{resource.id}", title: 'Follow this', class: "btn btn-turquoise-hover btn-sm" %>
	<% end %>
</span>

