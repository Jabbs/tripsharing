- flash.each do |name, msg|
  %div{class: "alert alert-#{name == :notice ? "success" : "error"}"}
    %a.close{"data-dismiss" => "alert", style: "padding-right: 1em;"} ×
    .div{style: "padding-left:1em;"}
      = msg
