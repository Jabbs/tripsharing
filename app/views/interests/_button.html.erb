<%= link_to user_interest_path(current_user.id, interest.id), class: "btn #{interest.has_it? ? "btn-success" : ""}", id: "identifier-#{interest.id}", method: :put, remote: true, style: "margin-right:5px; margin-top:10px;" do %>
	<%= Interest::IDENTIFIERS[interest.identifier] %>
<% end %>