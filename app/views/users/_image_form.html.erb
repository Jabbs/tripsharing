<%= form_for @user, multipart: true do |f| %>
	<%= f.hidden_field :user_id, value: @user.id %>
	<%= f.fields_for :image_attachments do |builder| %>
		<% if builder.object.new_record? %>
			<%= builder.file_field :image, style: "margin-left: 0px;", onchange: "PreviewImage();", id: "uploadImage" %>
		<% end %>
	<% end %>
	<br>
	<em>200x200px for optimal size.</em>
	<br>
	<br>
  <%= f.button "Save Image", data: { disable_with: "<i class='fa fa-spinner fa-spin'></i> Saving..." }, class: "btn btn-danger" %>

<% end %>